syntax = "proto3";

option csharp_namespace = "Beatport2Rss.TokenInterceptor";

service TokenService {
  rpc GetToken (Empty) returns (TokenReply);
}

message Empty {
}

message TokenReply {
  string access_token = 1;
}